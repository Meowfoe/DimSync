{
  "schemaVersion": 1,
  "id": "dimsync",
  "version": "${version}",
  "name": "DimSync",
  "description": "Keeps the whole group together: when any online player changes dimension (including modded ones), everyone else is pulled to that exact spot. Auto-discovers and remembers every dimension it has ever seen, assigning each a stable numeric ID.",
  "authors": [
    "you"
  ],
  "contact": {},
  "license": "CC0-1.0",
  "icon": "assets/dimsync/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.dimsync.DimSyncMod"
    ]
  },
  "mixins": [],
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": "~1.20.1",
    "java": ">=17",
    "fabric-api": "*"
  }
}
